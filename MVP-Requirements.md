# Background Remover - MVP 需求文档

## 项目概述

**产品名称**: Background Remover  
**目标用户**: 需要快速去除图片背景的用户（电商卖家、设计师、普通用户）  
**核心价值**: 免费、快速、无需注册即可使用的在线背景去除工具  
**技术栈**: 纯前端静态网站 + Remove.bg API  
**部署平台**: Cloudflare Pages（免费）

---

## 核心功能（MVP 阶段）

### 1. 图片上传
- 支持点击上传
- 支持拖拽上传
- 文件格式：PNG、JPG、JPEG
- 文件大小限制：10MB
- 前端验证文件类型和大小

### 2. 背景去除
- 调用 Remove.bg API 处理图片
- 显示处理进度（Loading 动画）
- 错误处理（API 失败、网络错误等）

### 3. 结果展示
- 左右对比显示：原图 vs 去背景后
- 响应式布局（移动端自动切换为上下布局）

### 4. 下载功能
- 一键下载去背景后的 PNG 图片
- 文件名：background-removed.png

### 5. 重新上传
- 处理完成后可以上传新图片
- 清空之前的结果

---

## 技术实现

### 前端架构
```
bg-remover/
├── index.html    # 主页面
├── app.js        # 核心逻辑
└── README.md     # 部署说明
```

### API 集成
- **服务商**: Remove.bg
- **免费额度**: 50次/月
- **调用方式**: 前端直接调用（CORS 支持）
- **API Key**: 需要用户自行申请并配置

### 数据流程
1. 用户上传图片 → 前端读取为 File 对象
2. 通过 FormData 发送到 Remove.bg API
3. 接收返回的 Blob 数据
4. 在内存中展示和下载（无服务器存储）

---

## 用户体验设计

### 视觉风格
- 渐变紫色背景（#667eea → #764ba2）
- 白色卡片容器，圆角设计
- 简洁现代的 UI

### 交互流程
```
上传图片 → 显示 Loading → 展示对比结果 → 下载/重新上传
```

### 响应式设计
- 桌面端：左右对比布局
- 移动端：上下堆叠布局
- 最小宽度支持：320px

---

## 部署方案

### Cloudflare Pages 部署步骤
1. 将代码推送到 GitHub 仓库
2. 登录 Cloudflare Dashboard
3. 进入 Pages → 创建项目
4. 连接 GitHub 仓库
5. 构建设置：
   - 构建命令：（留空）
   - 输出目录：/
6. 部署完成后获得 `.pages.dev` 域名

### 自定义域名（可选）
- 在 Cloudflare Pages 设置中添加自定义域名
- 自动配置 SSL 证书

---

## 变现策略

### MVP 阶段（免费）
- 完全免费使用
- 依赖 Remove.bg 免费额度（50次/月）
- 目标：验证需求、积累用户

### 后续迭代
1. **广告变现**: 集成 Google AdSense
2. **付费订阅**: 
   - 免费用户：每天 3 次
   - 付费用户：无限次 + 批量处理
3. **API 升级**: 切换到自建 AI 模型降低成本

---

## 成功指标（MVP）

- 部署成功并可访问
- 用户能成功上传图片并去除背景
- 用户能下载处理后的图片
- 移动端和桌面端体验良好
- 页面加载速度 < 2秒

---

## 风险与限制

### 技术风险
- Remove.bg API 免费额度有限（50次/月）
- 前端暴露 API Key 存在安全风险（后续需改为后端代理）

### 解决方案
- MVP 阶段接受 API Key 暴露风险
- 后续迭代时改为 Cloudflare Workers 代理 API 调用

---

## 开发时间估算

- 前端开发：✅ 已完成
- 本地测试：1小时
- 部署上线：30分钟
- **总计**: 1.5小时

---

## 下一步行动

1. ✅ 完成代码开发
2. ⏳ 申请 Remove.bg API Key
3. ⏳ 配置 API Key 到 app.js
4. ⏳ 本地测试功能
5. ⏳ 推送到 GitHub
6. ⏳ 部署到 Cloudflare Pages
7. ⏳ 测试线上环境
8. ⏳ 分享给用户验证

---

**文档版本**: v1.0  
**创建日期**: 2026-03-27  
**负责人**: shara
